file is very new and its first file
editing from the git website

I also wants to do from local
I want to do the changes before the checkout is merged
To make changes to a file locally before merging it into the main branch, you can follow these steps: